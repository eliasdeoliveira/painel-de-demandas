# Copie para .env e ajuste conforme o ambiente.
# Nenhum destes valores é segredo: são apenas os padrões de desenvolvimento.

DATABASE_URL=sqlite:///./zeeway.db
CORS_ALLOWED_ORIGINS=http://localhost:3000
